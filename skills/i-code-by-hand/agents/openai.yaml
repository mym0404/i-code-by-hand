interface:
  display_name: "I Code By Hand"
  short_description: "Resolve repo guidance before coding"
  default_prompt: "Use $i-code-by-hand to resolve repo guidance before coding, reviewing, debugging, or storing repo notes."

policy:
  allow_implicit_invocation: true
